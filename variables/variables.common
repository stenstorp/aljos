set +h
umask 022
export LC_ALL=POSIX

export BUILDROOT=$(pwd)
export LJOS=$(pwd)/aljos
export SOURCE_DIR=$(pwd)/source
export BUILD_DIR=$(pwd)/build
export COMPONENTS_DIR=$(pwd)/components
export LOG_DIR=$(pwd)/logs

export PATH=${LJOS}/cross-tools/bin:/bin:/usr/bin

source ${BUILDROOT}/config
source variables/${LJOS_ARCH}/variables
