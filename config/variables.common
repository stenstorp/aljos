set +h
umask 022
export LC_ALL=POSIX

export LJOS=$(pwd)/alj-os
export SOURCE_DIR=$(pwd)/source
export BUILD_DIR=$(pwd)/build
export COMPONENTS_DIR=$(pwd)/components

export THREADS=$(nproc)
export PATH=${LJOS}/cross-tools/bin:/bin:/usr/bin

# COMPATIBLE PLATFORMS:
# arm x86_64
export LJOS_PLATFORM=arm
source config/${LJOS_PLATFORM}/variables
